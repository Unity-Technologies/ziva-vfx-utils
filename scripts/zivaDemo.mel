global proc string zivaGetScriptPath()
{ // Retrieve ziva script path by running whatIs command,
  // It returns string like, "Mel procedure found in: <PATH TO THE ZIVA SCRIPT>"
  $rawZivaCmdScriptPath = `whatIs ZivaToggleEnabled`;
  $zivaCmdScriptPath = stringRemovePrefix($rawZivaCmdScriptPath, "Mel procedure found in: ");
  return dirname($zivaCmdScriptPath) + "/";
}

$demoScriptPath = zivaGetScriptPath() + "../demos/anatomicalArm.mel";
eval("source \"" + $demoScriptPath + "\"");

$demoScriptPath = zivaGetScriptPath() + "../demos/goaling.mel";
eval("source \"" + $demoScriptPath + "\"");

$demoScriptPath = zivaGetScriptPath() + "../demos/restScalePressure.mel";
eval("source \"" + $demoScriptPath + "\"");

$demoScriptPath = zivaGetScriptPath() + "../demos/attachmentTest.mel";
eval("source \"" + $demoScriptPath + "\"");

$demoScriptPath = zivaGetScriptPath() + "../demos/collisionTest.mel";
eval("source \"" + $demoScriptPath + "\"");

$demoScriptPath = zivaGetScriptPath() + "../demos/oneOfEachAttachments.mel";
eval("source \"" + $demoScriptPath + "\"");

$demoScriptPath = zivaGetScriptPath() + "../demos/oneOfEachCollisionTypes.mel";
eval("source \"" + $demoScriptPath + "\"");

$demoScriptPath = zivaGetScriptPath() + "../demos/spatiallyVaryingMaterials.mel";
eval("source \"" + $demoScriptPath + "\"");

$demoScriptPath = zivaGetScriptPath() + "../demos/cloth.mel";
eval("source \"" + $demoScriptPath + "\"");

$demoScriptPath = zivaGetScriptPath() + "../demos/isoMesher.mel";
eval("source \"" + $demoScriptPath + "\"");

$demoScriptPath = zivaGetScriptPath() + "../demos/selfCollisionsCacheAndDelaunayTetMesher.mel";
eval("source \"" + $demoScriptPath + "\"");

$demoScriptPath = zivaGetScriptPath() + "../demos/restScalePressure.mel";
eval("source \"" + $demoScriptPath + "\"");